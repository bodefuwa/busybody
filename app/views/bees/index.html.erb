<h1>Listing bees</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bees.each do |bee| %>
  <tr>
    <td><%= bee.name %></td>
    <td><%= bee.email %></td>
    <td><%= link_to 'Show', bee %></td>
    <td><%= link_to 'Edit', edit_bee_path(bee) %></td>
    <td><%= link_to 'Destroy', bee, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bee', new_bee_path %>
